name: Brain — pulseofproject.com Sync

# Pulls bugs / tasks (the "who does what" view) from pulseofproject.com and
# ingests them into brain_chunks. Direct ingest, no commit needed.

on:
  schedule:
    - cron: '30 4 * * *'  # 04:30 UTC = 10:00 IST, daily
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci
      - name: Sync pulseofproject.com issues
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PULSE_API_URL: ${{ secrets.PULSE_API_URL }}
          PULSE_API_KEY: ${{ secrets.PULSE_API_KEY }}
        run: node _Murali-Second-Brain/pulseofproject-sync.mjs
